{
  "name": "kishore-kumar-coaching",
  "private": true,
  "version": "2.0.0",
  "description": "Premium Next.js landing page for Kishore Kumar — Sports Psychology & Martial Arts Coach",
  "//lint": "`next lint` was removed in Next 16 — it parsed the word 'lint' as a directory argument and failed with 'Invalid project directory provided', so this project has had no working lint command. Until an ESLint 9 flat config is added, `lint` runs the type checker, which is the static analysis that actually exists here and is what CI gates on.",
  "scripts": {
    "dev": "next dev --hostname 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "//browserslist": "Without this, Next's default target ships ES5 helpers and polyfills for browsers no visitor uses — Lighthouse flags it as 'Legacy JavaScript'. These four lines cover ~96% of Indian mobile traffic while letting the compiler emit native async/await, optional chaining and classes.",
  "browserslist": [
    "chrome >= 87",
    "edge >= 88",
    "firefox >= 78",
    "safari >= 14"
  ],
  "dependencies": {
    "@radix-ui/react-accordion": "^1.2.2",
    "@radix-ui/react-slot": "^1.1.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "framer-motion": "^11.15.0",
    "lucide-react": "^0.469.0",
    "next": "^16.2.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^2.6.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.10",
    "tailwindcss": "^3.4.17",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
